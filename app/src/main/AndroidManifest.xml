<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passwordmanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_lock_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_lock_24"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PasswordRandomGeneration"></activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreatePasswordActivity" />
        <activity android:name=".LoginPage" />
        <activity android:name=".AccountList" />
        <activity android:name=".AddAccount" />
    </application>

</manifest>